Code 文件夹中文件命名规则如下：
GSh01为h=0.1时的Gauss-Seidel迭代；
Jach01为h=0.1时的Jacobi迭代；
Jach005为h=0.05时的Jacobi迭代；
Jach002为h=0.02时的Jacobi迭代；
Jach001为h=0.01时的Jacobi迭代；
SORw09为h=0.1,w=0.9的SOR迭代；
SORw12为h=0.1,w=1.2的SOR迭代；
SORw13为h=0.1,w=1.3的SOR迭代；
SORw19为h=0.1,w=1.9的SOR迭代；

注：请使用Matlab运行上述程序！

2022.04更新备注：由于当初写报告不会使用latex插入代码块，代码部分使用截图插入报告内容，可能对阅读带来了些许麻烦。
